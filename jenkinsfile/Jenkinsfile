pipeline {
	agent {label 'jnlp-slave'}
	stages {
		stage('Build') {
			steps {
				sh 'pwd;ls'
				sh 'docker build -t helloJenkins:1.0 -f /home/jenkins/workspace/pipelineLearning/dockerfile/Dockerfile'
			}
		}
	}
}
